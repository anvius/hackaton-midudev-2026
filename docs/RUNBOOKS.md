# RUNBOOKS

## Local Development

- `make dev` to run frontend and backend
- `make test-all` to run baseline tests
- `make start-infra` to start containers

## Incident Basics

- Check logs under `var/log/`
- Validate backend health endpoint `/health`
